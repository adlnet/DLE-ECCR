kind: PersistentVolumeClaim
apiVersion: v1
metadata:
   labels:
      app: eccr-cds
   name: cds-storage-claim
spec:
   accessModes:
      - "ReadWriteOnce"
   resources:
      requests:
         storage: "1Gi"
